.row
  .span8
    .hero-unit.location-page{:style => "background: url(#{@location.image}); background-size: cover"}
      %h1
        %span.bg= @location.name
      %p
        %span.bg Find inspiration in the city that never sleeps.
- if @location.challenges.any?
  .big-challenge
    %h6 Top Challenge
    - challenge = @location.challenges.first
    %h3= link_to challenge.name, challenge
    %p= challenge.description
    .thumbnail-hider
      .row-thumbnails
        - challenge.photos(@graph, current_user, challenge).each do |photo|
          = link_to photo do
            = image_tag photo.image(:thumb)
      .more-photos
        
  - if @location.challenges.count > 1
    %h6 More Challenges
    .row
      - @location.challenges.slice(1, @location.challenges.count).each do |challenge|
        .span4
          %h3= link_to challenge.name, challenge
          %p= challenge.description
  

%hr

%p
  %b Places of interest:
  - @venues.each do |venue| 
    %p
      = venue.name
      = venue.rating
  =@url


= link_to 'Edit', edit_location_path(@location)
\|
= link_to 'Back', locations_path


= link_to "Add a Challenge Here", new_challenge_path(:location_id => @location.id)
