%h2 What to do?
-if @challenges_todo.any?
  .todo
    %h3 On your todo list:
    .todo-challenges
      -@challenges_todo.each do |todo|
        = link_to todo.name, todo
-if @friend_challenges.any?
  .friends
    %h3 Challenges your friends have completed:
    .friend-challenges
      -@friend_challenges.each do |challenge|
        = link_to challenge[:challenge].name, challenge[:challenge]
        Completed by
        = link_to challenge[:friend].username, challenge[:friend]
-if @popular_challenges.any?
  .popular
    %h3 Other popular challenges:
    .popular-challenges
      -@popular_challenges.each do |popular|
        = link_to popular.name, popular
        = popular.vote_value

